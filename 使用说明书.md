# BOSS Local Capture 本地招聘工作台使用说明书

这是一套运行在 Windows 本机的招聘辅助工具。当前 M1 预览版的重点是把“浏览器插件采集、Web 入库、候选人查看、来源历史、批次筛选、批次快照和 Markdown 导出”放到一个可恢复、可追踪、可阅读的本地工作台里。

## 一句话说明

用 Chrome 扩展从 Boss / 猎聘页面采集候选人，网页工作台负责入库、去重、候选人查看、来源历史、批次筛选和 Markdown 导出。旧桌面端仍作为兼容入口保留，但不是新用户默认入口。

## 主要功能

### 1. 候选人采集

- 支持 Boss 推荐人才页面采集。
- 支持猎聘推荐人才页面采集。
- 使用普通 Chrome 登录和浏览，避免自动化浏览器被网站拦截。
- 通过 Chrome 扩展将页面候选人卡片发送到本机网页工作台。
- 自动去重，同一个候选人重复采集不会反复生成脏数据。

### 2. 候选人工作台

- 候选人可以没有正式岗位，先进入统一候选人库。
- 支持按姓名、来源岗位文字和原始卡片关键词搜索。
- 支持来源平台筛选、只看未绑定岗位和最近采集时间排序。
- 候选人详情只读展示基础信息、来源链接、原始快照和来源出现历史。
- 来源出现历史来自采集批次快照，不会被后续主档更新覆盖。

### 3. 最近批次

- 批次按最近采集时间倒序展示。
- 支持来源平台、批次状态、只看失败批次和只看今天采集筛选。
- 支持直接打开指定批次，即使该批次不在当前第一页。
- 批次详情展示不可变快照，并可连续查看候选人原始快照。
- 批次列表和批次详情都可导出 Markdown。

### 4. 人才库

- 候选人会沉淀为本机主档。
- 候选人主档和每次采集快照分开保存。
- 同一个候选人可以在多个批次中出现。
- 原始快照只在候选人详情、批次详情和用户主动导出的 Markdown 中展示，不会出现在候选人列表里。

### 5. 后续阶段边界

AI 初筛、人工复核、招聘进展、Mapping、简历管理和沟通记录属于后续阶段或旧设计说明，当前 M1 预览版不把这些作为已封板功能。

### 6. 导出

支持导出：

- Markdown：适合快速阅读、汇报和复盘。
- CSV / JSONL：旧桌面兼容入口可能仍有相关导出说明；M1 网页工作台默认以批次 Markdown 导出为主。

## 项目位置和启动方式

项目目录：

```text
<项目目录>
```

首次初始化：

```text
<项目目录>\setup_web_workbench.cmd
```

日常启动网页工作台：

```text
<项目目录>\launch_web_workbench.cmd
```

双击启动文件后会出现可见启动窗口，完成配置、资源、端口、人才库和浏览器打开检查。旧桌面入口 `launch_boss_local_tool.cmd` 只作为兼容入口保留，不是新用户默认 onboarding。

首次启动后会生成：

```text
<用户选择的数据目录>\boss_local_tool.db
<用户选择的数据目录>\exports\
<本机运行目录>\web-launcher.log
```

## 第一次使用

### 第一步：初始化网页工作台

首次使用先双击：

```text
<项目目录>\setup_web_workbench.cmd
```

初始化完成后，再双击：

```text
<项目目录>\launch_web_workbench.cmd
```

启动窗口会打开浏览器中的网页确认页。确认本地服务和人才库就绪后，点击“进入工作台”。

### 第二步：生成插件连接码

进入网页工作台“设置”页面，点击生成插件连接码，并复制到 Chrome 扩展。

连接码只用于一次性配对，成功后失效。内部凭证不需要用户手工填写，也不会出现在 URL、普通页面、插件状态文案或启动日志里。

### 第三步：安装 Chrome 扩展

1. 打开 Chrome。
2. 进入：

```text
chrome://extensions
```

3. 打开右上角“开发者模式”。
4. 点击“加载已解压的扩展程序”。
5. 选择文件夹：

```text
<项目目录>\extension
```

6. 建议把扩展固定到 Chrome 工具栏，方便采集。

### 旧桌面兼容入口

如果你需要使用旧桌面端功能，可以双击：

```text
<项目目录>\launch_boss_local_tool.cmd
```

网页工作台和旧桌面端都会使用本机数据库锁，不能同时运行。请先关闭另一个入口，再启动当前入口。

## 采集候选人

### 推荐：网页工作台模式

1. 先通过 `launch_web_workbench.cmd` 启动网页工作台。
2. 在网页工作台“设置”页面生成插件连接码。
3. 在 Chrome 扩展中粘贴连接码并完成配对。
4. 打开 Boss 或猎聘页面，使用扩展采集当前已加载候选人，或自动滚动后采集。
5. 采集完成后，候选人会通过 Web intake 自动进入本机人才库。
6. 回到网页工作台，在“候选人”和“最近批次”页面查看本次入库结果。
7. 需要交接或复盘时，在批次列表或批次详情中导出 Markdown。

### 采集质量检查

M1 预览版主要提供批次接收、失败数量、部分成功状态和批次详情查看。下面这些更细的质量分流属于后续阶段或旧桌面说明，当前不要把它当成默认网页工作台流程：

- “可进入 AI 初筛”：数量、核心字段和来源检查通过；
- “建议补充采集”：数据可以继续使用，但人数不足、重复率偏高或字段完整度不足；
- “需要人工检查”：没有可用候选人、来源不一致、关键字段严重缺失或批次异常；
- “采集中，完成后检查”：当前批次尚未结束，应等待采集完成。

质量检查只提供提示，不会删除数据、自动重采或强制阻止 AI 初筛。

日常只需要保留招聘平台和网页工作台两个主要窗口。外部 AI 网页不是正式流程的一部分；AI 初筛、人工复核和 Mapping 等能力属于后续阶段。

### 普通采集流程

1. 启动网页工作台。
2. 打开 Chrome，手动登录 Boss 或猎聘。
3. 进入推荐人才页面。
4. 打开 Chrome 扩展弹窗。
5. 把网页工作台生成的一次性连接码粘贴到插件连接框。
6. 点击“连接并记住”，看到 Web 工作台已连接。
7. 可选填写来源岗位文字，然后点击：

```text
Collect Current Page
```

或：

```text
Auto Scroll + Collect
```

采集完成后，候选人会自动进入网页工作台使用的本地数据库。

### 推荐用法

如果页面候选人较多，优先使用：

```text
Auto Scroll + Collect
```

它会自动滚动页面，尽量加载更多候选人后再采集。

## AI 初筛（后续阶段 / 旧桌面说明）

下面内容来自旧桌面规划和兼容说明。当前 M1 release candidate 的默认流程不包含 AI 初筛封板能力；新用户请先使用网页工作台完成采集、候选人查看、来源历史、批次筛选和 Markdown 导出。

### 第一步：创建岗位筛选方案

打开桌面端“AI 初筛”页面：

1. 填写岗位名称。
2. 粘贴或上传 JD。
3. 系统会生成默认 Prompt。
4. 可以按需要编辑 Prompt。
5. 保存筛选方案。

注意：系统会拦截不适合用于自动招聘判断的条件，例如年龄、性别、婚育、健康、照片、外貌等。

### 第二步：选择候选人范围

可以按岗位、批次或候选人范围选择要筛选的人。

建议先用小批量试跑，例如 10 到 30 人，确认 Prompt 和结果符合预期后再跑大批量。

### 第三步：配置 AI 服务

支持：

- OpenAI
- DeepSeek
- 自定义 OpenAI 兼容接口

在页面里选择服务商、模型，并填写 API Key。

API Key 只保存在当前进程内，不会写入配置文件或数据库。

### 第四步：开始筛选

点击“开始 AI 初筛”。

筛选过程中可以看到：

- 总任务数
- 已完成
- 失败数
- 待处理
- 人工确认
- 暂缓
- 当前候选人结果

如果程序中途关闭，重新打开后可以继续任务。

## 自动化采集 + AI 初筛（后续阶段 / 旧桌面说明）

这部分不是当前 M1 网页工作台默认能力，仅作为旧桌面说明或后续阶段参考。

### 设置自动化流程

1. 先在“AI 初筛”页面保存岗位筛选方案。
2. 打开“自动化流程”页面。
3. 选择筛选方案。
4. 填写采集岗位名、页面地址、AI 服务商、模型。
5. 保存设置。
6. 启用自动化。

### 在 Chrome 扩展中启动

1. 打开 Boss 或猎聘推荐人才页面。
2. 打开 Chrome 扩展。
3. 点击：

```text
AUTO: Scroll + Collect + AI Screen
```

旧桌面兼容模式中，扩展可能继续保留相关入口；网页工作台模式当前只负责采集入库、候选人查看、批次快照和 Markdown 导出，不会自动进入 AI 初筛。

## 查看和管理候选人

打开“候选人”页面，可以：

- 按关键词搜索。
- 按来源平台筛选。
- 只看未绑定岗位候选人。
- 按最近采集时间排序。
- 查看候选人详情。
- 查看来源出现历史。
- 从来源历史跳转到对应批次。
- 查看原始卡片快照。

推荐日常流程：

1. 先用搜索和来源平台筛选定位候选人。
2. 打开详情，阅读基础信息、来源链接和原始快照。
3. 查看来源出现历史，确认候选人来自哪个平台、哪个批次、什么时间。
4. 需要复盘时跳转到批次详情或导出 Markdown。

## 人工复核（后续阶段 / 旧桌面说明）

下面内容不属于当前 M1 网页工作台已封板能力，仅作为后续阶段方向或旧桌面说明保留。

打开“人工复核”页面，可以处理：

- 信息不足的候选人。
- AI 置信度低的候选人。
- AI 识别到风险的候选人。
- AI 失败的候选人。
- 需要人工判断是否继续推进的人。

页面顶部显示待复核、高优先级、AI 不确定、资料不完整、待补充、已暂缓和今日已复核人数。可以按岗位及以下队列筛选：

- 优先复核
- 普通待复核
- 待补充资料
- 已暂缓

待办按高评级、AI 不确定、AI 失败和风险优先排列，并显示优先原因。

复核后可以标记：

- 通过人工复核
- 人工复核拒绝
- 放入人才库
- 标记为待补充资料
- 暂缓处理
- 更新招聘状态

点击快速操作或“保存并处理下一位”后，系统保存原因、备注和时间，保留历史记录，并自动选择下一位待办。

## AI 与人工结果对照（后续阶段 / 旧桌面说明）

下面内容不属于当前 M1 网页工作台已封板能力，仅作为后续阶段方向或旧桌面说明保留。

当候选人已经在“人工复核”中形成明确的“通过”或“拒绝”结论后，可以打开“AI 对照”页面进行复盘。

页面支持按岗位及以下对照结果筛选：

- 判断一致：AI 建议推进且人工通过，或 AI 不建议推进且人工拒绝。
- 判断有差异：包括 AI 高估和 AI 低估。
- 人工定案：AI 原本不确定，由人工完成通过或拒绝判断。

顶部概览显示已对照、一致、差异、人工定案、人工通过、人工拒绝和一致率。这里的一致率只计算 AI 已经明确建议推进或不建议推进的样本，AI 不确定样本不会混入分母。

选择某条记录后，可以同时查看：

- AI 评级、置信度、建议动作和人物画像。
- AI 使用的证据、识别的缺口及风险。
- 人工结论、具体备注和复核时间。
- AI 高估、AI 低估、判断一致或人工定案的分类原因。

点击“在候选人页查看完整档案”可以继续检查该候选人的完整资料与招聘轨迹。

当前对照结果用于校准招聘判断，不能直接当作模型准确率或最终招聘成功率。

## 下一步动作及提醒（后续阶段 / 旧桌面说明）

下面内容不属于当前 M1 网页工作台已封板能力，仅作为后续阶段方向或旧桌面说明保留。

左侧“行动待办”页面用于集中处理招聘推进事项。待办可以关联一个候选人岗位关系或一个招聘任务。

创建候选人待办：

1. 打开“候选人”页面并选择候选人。
2. 在候选人详情中选择对应岗位。
3. 点击“创建下一步动作”。
4. 填写动作类型、标题、负责人、截止时间、优先级和备注。

创建招聘任务待办：

1. 打开“招聘任务”页面并选择任务。
2. 点击“创建下一步动作”。
3. 填写行动信息并保存。

行动待办支持以下视图：

- 全部待办
- 今日
- 逾期
- 未来七天
- 已完成
- 全部记录

选择待办后可以编辑、完成、取消，或点击“打开关联对象”返回候选人或招聘任务。完成和取消的记录仍会保留。

仪表盘会显示待处理、今日、逾期、未来七天和今日完成数量。当前只提供应用内提醒，不会弹出 Windows 系统通知，也不会自动联系候选人。完成待办不会自动改变候选人招聘阶段或招聘任务状态。

## 招聘漏斗（后续阶段 / 旧桌面说明）

下面内容不属于当前 M1 网页工作台已封板能力，仅作为后续阶段方向或旧桌面说明保留。

打开“仪表盘”页面，可以查看：

- 已采集人数
- 已筛选人数
- 已联系人数
- 已回复人数
- 面试人数
- Offer 人数
- 入职人数
- 拒绝人数
- 人才库人数

也可以按 AI 评级看转化，例如：

```text
SSR 候选人有多少被联系、多少回复、多少进面、多少入职
```

这部分用于复盘 AI 筛选质量和招聘动作效果。

## 导出数据

M1 网页工作台默认在“最近批次”列表和批次详情中导出 Markdown。导出内容来自不可变批次快照，不会被后续候选人主档变化污染。

### CSV

适合 Excel/WPS 查看。

### JSONL

适合给脚本、AI、数据处理工具继续分析。

### Markdown

适合快速阅读、给团队同步、做招聘复盘。

旧桌面兼容导出目录通常在：

```text
<项目目录>\data\exports
```

## 常见问题

### 1. 为什么不能直接在桌面端打开 Boss 采集？

Boss 对自动化浏览器限制比较强。当前方案使用普通 Chrome 手动登录，再通过扩展采集页面内容，更稳定。

### 2. 扩展提示本地接口连接失败怎么办？

检查：

- 网页工作台是否已经通过 `launch_web_workbench.cmd` 启动。
- 网页确认页是否显示本地服务和人才库就绪。
- 在网页工作台“设置”页面重新生成一次性连接码，并在扩展中重新配对。
- 如果提示凭证不正确，重新生成连接码，不要手工填写或修改内部凭证。
- 更新扩展后，是否在 `chrome://extensions` 点击过“重新加载”。
- 本地端口是否被其他程序占用。

### 3. 旧桌面 AI 筛选失败怎么办？

先看失败信息，常见原因有：

- API Key 不正确。
- 网络超时。
- 模型服务限流。
- 自定义接口地址不正确。

失败任务不会丢，可以在 AI 初筛页面重试。

### 4. 中途关机了怎么办？

重新启动网页工作台后，已经成功入库的批次和候选人仍在本机人才库中。插件 Web intake 使用幂等请求，重复发送同一批次不会重复创建候选人或快照。

### 5. 会不会重复扣模型费用？

系统会按岗位、候选人、模型、Prompt 版本和请求内容生成哈希。同一请求已经有成功结果时，会优先复用缓存。任务本身也保存请求快照，继续旧任务时不会因为岗位方案后来被编辑而误触发重复调用。

### 6. 数据放在哪里？

主要数据：

```text
<用户选择的数据目录>\boss_local_tool.db
```

日志：

```text
<本机运行目录>\web-launcher.log
```

导出：

```text
<用户选择的数据目录>\exports
```

## 推荐日常使用流程

```text
运行 setup_web_workbench.cmd 完成首次初始化
→ 运行 launch_web_workbench.cmd 启动网页工作台
→ 打开 Chrome 并登录招聘网站
→ 在网页工作台设置页生成插件连接码
→ 在扩展中连接并记住
→ 用扩展采集当前已加载或自动滚动采集
→ 在候选人页搜索、筛选和查看详情
→ 在最近批次页筛选、查看快照
→ 导出批次 Markdown 做复盘
```

## 当前适合的使用场景

- 每天从 Boss / 猎聘推荐页批量收候选人。
- 对同一来源岗位进行多批次采集和对比。
- 想避免手工 CSV 来回导出、重复整理、批次来源混乱。
- 想沉淀一个可搜索的人才库。
- 想追溯候选人来自哪个平台、哪个批次、什么时间。

## 使用建议

- 先用小批量采集确认页面和插件连接正常。
- 采集后优先查看候选人详情和原始快照，确认来源信息是否完整。
- 对重要批次及时导出 Markdown 做留档。
- AI 初筛、人工复核、Mapping、简历和沟通记录等能力在后续阶段上线后，再纳入正式流程。
