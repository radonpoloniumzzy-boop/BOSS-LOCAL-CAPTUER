# 当前预览版本说明

## 对应代码基线

- Git 分支：`main`
- 正式下载基准：GitHub `main`
- 具体提交：以当前 `main` HEAD 或正式 release/tag 为准
- M2-A 岗位与招聘任务基础：以当前 `main` HEAD 或正式 release/tag 为准
- M1 候选人工作台验收准备分支：`codex/m1-candidate-detail-readability`
- M0 开始时的审计基线：`d5cb9cadb9cf4e0f0167451b15a12c4f42a01652`

## 组件版本来源

- 核心工作台版本：`core/version.py`
  - 当前值：`0.3.0-web-phase-2c`
- Chrome 插件版本：`extension/manifest.json`
  - 当前值：`0.5.1`
- 前端包版本：`web/frontend/package.json`
  - 当前值：`0.1.0`

这些版本属于不同组件，不自动合并成一个统一产品号。

## 当前稳定可用功能

- 网页工作台启动与首次设置
- 本地人才库目录确认
- Chrome 插件连接码配对
- 招聘平台页面采集
- Web intake 自动入库
- 候选人列表搜索、来源平台筛选、未绑定筛选、排序、分页和刷新
- 候选人只读详情抽屉
- 候选人来源出现历史
- 原始卡片快照预览、展开、收起和复制
- 最近采集批次倒序查看
- 批次状态、失败、今天和来源平台筛选
- 批次 ID 直接打开
- 批次详情不可变快照查看
- 批次快照上一位、下一位连续阅读
- 批次 Markdown 导出
- 岗位档案列表、新建、编辑、状态切换和版本历史
- 招聘任务创建、状态切换和只读详情
- 插件当前招聘任务上下文
- 网页端与旧桌面端的人才库互斥保护

## 新用户试用路径

1. 下载 GitHub `main`。
2. 双击 `setup_web_workbench.cmd` 初始化本项目内的运行环境。
3. 双击 `launch_web_workbench.cmd` 启动网页工作台。
4. 首次打开时确认一个长期保留的人才库目录。
5. 在网页“设置”复制一次性连接码。
6. 在 Chrome 加载项目里的 `extension/` 目录。
7. 在插件中粘贴连接码并完成配对。
8. 可选：在网页“岗位”建立岗位档案和招聘任务，并设为插件当前上下文。
9. 在招聘平台页面执行采集。
10. 回到网页查看候选人、来源历史、批次详情和 Markdown 导出。

网页模式不要求用户手工填写 API 地址或 Token。

## M1 验收关注点

- 候选人列表 API 只返回列表所需字段，不暴露原始卡片、来源 URL、详情 URL、AI 字段、人工复核字段或状态历史。
- 候选人详情 API 只返回 M1 详情页明确展示的只读字段。
- 来源出现历史来自 `capture_batch_items` 和采集批次，不通过当前候选人主档反推历史。
- 来源历史接口使用轻量候选人存在性查询，不调用旧桌面端重量级详情链路。
- `today_only` 批次筛选在单次请求内共用同一本机业务日期边界。
- Markdown 导出读取 `capture_batch_items` 不可变快照，后续主档变化不会污染旧批次导出。
- 网页和插件 Web 模式共用 `/api/capture-batches/{batch_id}/export.md`。
- Token、连接码和内部鉴权头不会作为用户可见信息暴露；候选人原始快照只在详情、批次阅读和导出场景按用户操作展示。
- Drawer 使用真实模态语义，支持焦点陷阱、Escape 顶层关闭、focusin 守卫、背景滚动锁和隐藏页面清理。
- `npm.cmd run qa:readability` 使用真实浏览器覆盖 `1920x1080`、`1440x900`、`1280x720`、`760x720`，并验证 reduced-motion 与 focus-visible。

## M2-A 验收关注点

- 岗位档案 Web API 只返回岗位基础字段，不暴露内部 Prompt、AI 配置或原始评估字段。
- 岗位编辑需要 `expected_version`，版本冲突返回稳定错误；无变化保存不会生成新版本。
- 岗位版本记录不可变，招聘任务固定创建时的岗位版本。
- 招聘任务只能基于招聘中的岗位和存在的岗位版本创建。
- 插件当前任务上下文只保存在本机配置中，不写入连接码，也不复制 Token。
- 只有执行中的招聘任务可以设为插件当前任务；待启动、暂停、已完成、已取消或失败的任务不会作为插件上下文。
- 当前任务不可用、任务结束或岗位关闭时，插件上下文变为不可用；关闭岗位会取消该岗位下尚未终结的招聘任务；无上下文时仍允许无岗位采集。
- 旧桌面兼容入口和 Web 工作台入口仍保持互斥，不引入双写。

## 当前未完成功能

以下内容仍属于后续阶段，不应在当前预览中承诺为已完成：

- 人才 Mapping
- AI 初筛
- 人工复核闭环
- 简历分类归档
- 沟通记录与招聘推进全链路

岗位档案和招聘任务基础已进入当前预览；岗位的 AI 筛选、人工复核、沟通推进和 Mapping 仍属于后续阶段。

## 已知边界

- Chrome 插件仍是招聘平台页面采集入口。
- 网页工作台是当前默认主入口。
- 旧桌面端只作为兼容入口保留。
- 程序不会在数据库缺失时创建空库替代旧库。
- 网页端和旧桌面端不能同时占用同一人才库。

## 关于旧的“紧凑型分屏工作台”Issue

旧 Issue 关注的是桌面端在半屏与紧凑布局下的可用性。

现在主路线已经切到网页工作台，所以它没有完全失效，但优先级和适用面已经被网页路线明显替代：

- 如果目标是当前正式试用版本的日常使用体验，应该优先推进网页工作台；
- 如果目标仍是维护旧桌面兼容入口，那条 Issue 仍有参考价值。
